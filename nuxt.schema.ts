import { field, group } from '@nuxt/content/preview'

export default defineNuxtSchema({
  appConfig: {
    // Navigation is now handled through content.config.ts and will appear in the content tab
  }
}) 